#论事件驱动与异步io
通常 我们写服务器处理模型的程序时 有以下几种模型：
1. 每收到一个请求 创建一个新的进程 来处理该请求
2. 每收到一个请求 创建一个新的线程 来处理该请求
3. 每收到一个请求 放入一个事件列表 让主进程通过非阻塞io方式来处理请求

以上几种方式 各有不同
第一种方法 由于创建新的进程开销比较大 所以 会导致服务器性能比较差 但实现比较简单
第二种方法 由于要涉及到线程的同步 有可能会面临死锁的问题
第三种方法 在写应用程序代码时 逻辑比前两种要复杂


10-14

appall
D2@b54ct451123f7